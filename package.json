{
  "name": "cypress-header-bug-minimal",
  "version": "1.0.0",
  "description": "Reproduction of a bug with cy.intercept and the Content-Length header",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "dev": "nodemon app.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "prettier:fix": "prettier --write .",
    "cypress:open": "cypress open",
    "cypress:run": "cypress run"
  },
  "author": "Austin Bourgerie",
  "license": "MIT",
  "devDependencies": {
    "cypress": "^10.11.0",
    "nodemon": "^2.0.20",
    "prettier": "^2.7.1"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
